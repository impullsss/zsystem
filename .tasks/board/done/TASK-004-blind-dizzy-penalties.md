# TASK-004: Штрафы Слепоты и Головокружения

**Статус:** backlog
**Приоритет:** high
**Создана:** 2026-03-23

## Описание

- **Слепота:** блокировать ползунок прицеливания в UI — нельзя прицеливаться.
- **Головокружение:** резать итоговый шанс попадания на 50%.

## Критерии готовности

- [ ] При статусе Blind: ползунок `#aim-slider` задизейблен, select `#aim-location` заблокирован
- [ ] При статусе Blind: выводить предупреждение "Вы слепы — прицеливание недоступно"
- [ ] При статусе Dizzy: итоговый `totalChance` умножается на 0.5 перед броском
- [ ] Эффект Dizzy отображается в чат-сообщении броска

## Технические заметки

- UI ползунка: `src/module/dice.js` — функция `performAttack()` (строка 332), HTML-строка с `#aim-slider` (≈ строка 393)
- Шанс попадания: переменная `totalChance` в `_executeAttack()` (≈ строка 544)
- Статус blind: `actor.statuses.has("blind")` — id `"blind"` в `constants.js`
- Статус dizzy: `actor.statuses.has("dizzy")` — id `"dizzy"` в `constants.js`
- Блокировку слайдера добавить при рендере диалога атаки (проверять статусы актора перед показом диалога)
