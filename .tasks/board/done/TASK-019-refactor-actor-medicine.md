# TASK-019 — Рефакторинг: вынести логику медицины из actor.js

**Статус:** backlog
**Приоритет:** low
**Размер:** S (1–2 часа)
**Зависит от:** TASK-018 (желательно делать после)

## Цель

Вынести логику лечения из `actor.js` в `src/module/actor-medicine.js`.

## Что переносится

- `useMedicine(item)` (~строки 1020–1083)
- `applyMedicineLogic(healer, itemData, limbKey)` (~строки 1084–1162)
- `_reportHealing(healer, messages, limb, itemName)` (~строки 1163–1177)

## Результат

- `actor.js` теряет ещё ~160 строк
- Методы в ZActor — тонкие обёртки
- Публичный API не меняется

## Проверка

- Использование аптечки лечит HP и конечности
- Отчёт о лечении появляется в чате
- Антибиотик снижает стадию инфекции
