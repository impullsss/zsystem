<form class="{{cssClass}}" autocomplete="off">
    <header class="sheet-header">
        <img class="profile-img-box" src="{{actor.img}}" data-edit="img" title="{{actor.name}}"/>
        <div style="flex:1;">
            <input class="charname-input" name="name" type="text" value="{{actor.name}}" placeholder="Название"/>
        </div>
    </header>

    <nav class="sheet-tabs tabs" data-group="primary">
        <a class="item" data-tab="loot">Содержимое</a>
        {{#if isGM}}<a class="item" data-tab="config" style="color:#d84315;"><i class="fas fa-cogs"></i> GM</a>{{/if}}
    </nav>

    <div class="sheet-body">
        
        <!-- LOOT TAB -->
        <div class="tab loot" data-group="primary" data-tab="loot">
            
            {{#if attr.isLocked.value}}
                {{#if isGM}}
                    <div style="background:#fff3e0; padding:5px; margin-bottom:10px; border:1px dashed orange;">
                        <i class="fas fa-lock"></i> <b>GM VIEW:</b> Игроки видят замок.
                    </div>
                {{else}}
                    <!-- ЭКРАН ЗАМКА -->
                    <div class="harvest-lock-screen">
                        <i class="fas fa-lock" style="font-size:64px; color:#555; margin-bottom:20px;"></i>
                        <h2>ЗАПЕРТО</h2>
                        
                        <div class="grid grid-3col" style="gap:10px; margin-top:20px;">
                            <button type="button" class="try-key">
                                <i class="fas fa-key"></i> Ключ
                            </button>
                            
                            {{#if attr.canPick.value}}
                            <button type="button" class="try-pick">
                                <i class="fas fa-tools"></i> Взлом
                            </button>
                            {{else}}
                            <button disabled style="opacity:0.5; cursor:not-allowed;">
                                <i class="fas fa-ban"></i> Не взломать
                            </button>
                            {{/if}}

                            {{#if attr.canBash.value}}
                            <button type="button" class="try-bash">
                                <i class="fas fa-fist-raised"></i> Выбить
                            </button>
                            {{else}}
                            <button disabled style="opacity:0.5; cursor:not-allowed;">
                                <i class="fas fa-ban"></i> Не выбить
                            </button>
                            {{/if}}
                        </div>
                    </div>
                {{/if}}
            {{/if}}

            {{#if showInventory}}
                
                <!-- ЛОВУШКА (Если обнаружена) -->
                {{#if (and attr.isTrapped.value attr.trapActive.value)}}
                    {{#if trapDetected}}
                        <div style="background:#ffebee; border:2px solid red; padding:10px; margin-bottom:10px; text-align:center;">
                            <h3 style="color:red; margin:0;"><i class="fas fa-exclamation-triangle"></i> ОБНАРУЖЕНА ЛОВУШКА!</h3>
                            <button type="button" class="try-disarm" style="background:#e57373; color:white; margin-top:5px;">Обезвредить</button>
                        </div>
                    {{else}}
                        {{#if isGM}}<div style="font-size:0.8em; color:gray; text-align:center;">(Ловушка скрыта)</div>{{/if}}
                    {{/if}}
                {{/if}}

                <div class="section-header">
                    <span>Предметы</span>
                    {{#if isGM}}<a class="item-create"><i class="fas fa-plus-circle"></i></a>{{/if}}
                </div>
                
                <ol class="light-item-list">
                {{#each inventory as |section key|}}
                    {{#if section.items.length}}
                    <li style="font-size:0.8em; color:#777; margin-top:5px;">{{section.label}}</li>
                    {{#each section.items as |item|}}
                        <li class="light-item" data-item-id="{{item.id}}">
                            <img src="{{item.img}}" width="24" height="24"/>
                            <div class="item-name">{{item.name}}</div>
                            <div class="item-qty">x{{item.system.quantity}}</div>
                            {{#if ../../isGM}}
                            <div class="item-controls">
                                <a class="item-delete"><i class="fas fa-trash"></i></a>
                            </div>
                            {{/if}}
                        </li>
                    {{/each}}
                    {{/if}}
                {{/each}}
                </ol>
            {{/if}}
        </div>

        <!-- GM CONFIG TAB -->
        {{#if isGM}}
        <div class="tab config" data-group="primary" data-tab="config">
            <h3>🔒 Замок</h3>
            <div class="form-group"><label>Заперто?</label><input type="checkbox" name="system.attributes.isLocked.value" {{checked attr.isLocked.value}}/></div>
            <div class="form-group"><label>Название Ключа</label><input type="text" name="system.attributes.keyName.value" value="{{attr.keyName.value}}"/></div>
            
            <div class="grid grid-2col">
                <div class="form-group">
                    <label>Можно взломать?</label>
                    <input type="checkbox" name="system.attributes.canPick.value" {{checked attr.canPick.value}}/>
                </div>
                <div class="form-group">
                    <label>Сложность (СЛ)</label>
                    <input type="number" name="system.attributes.lockDC.value" value="{{attr.lockDC.value}}"/>
                </div>
            </div>

            <div class="grid grid-2col">
                <div class="form-group">
                    <label>Можно выбить?</label>
                    <input type="checkbox" name="system.attributes.canBash.value" {{checked attr.canBash.value}}/>
                </div>
                <div class="form-group">
                    <label>СЛ Выбивания</label>
                    <input type="number" name="system.attributes.bashDC.value" value="{{attr.bashDC.value}}"/>
                </div>
            </div>

            <hr>
            <h3>💣 Ловушка</h3>
            <div class="form-group"><label>Есть ловушка?</label><input type="checkbox" name="system.attributes.isTrapped.value" {{checked attr.isTrapped.value}}/></div>
            <div class="form-group"><label>Активна?</label><input type="checkbox" name="system.attributes.trapActive.value" {{checked attr.trapActive.value}}/></div>
            <div class="grid grid-2col">
                <div class="form-group"><label>Урон</label><input type="text" name="system.attributes.trapDmg.value" value="{{attr.trapDmg.value}}"/></div>
                <div class="form-group"><label>Радиус (м)</label><input type="number" name="system.attributes.trapSpotRadius.value" value="{{attr.trapSpotRadius.value}}"/></div>
                <div class="form-group"><label>Обнаружение (СЛ)</label><input type="number" name="system.attributes.trapDC.value" value="{{attr.trapDC.value}}"/></div>
                <div class="form-group"><label>Обезвреж. (СЛ)</label><input type="number" name="system.attributes.disarmDC.value" value="{{attr.disarmDC.value}}"/></div>
            </div>

            <hr>
            <h3>👻 Схрон (Скрыто)</h3>
             <div class="form-group">
                <label>Скрыт на сцене?</label>
                <input type="checkbox" name="system.attributes.isHidden.value" {{checked attr.isHidden.value}}/>
                <p class="notes" style="font-size:0.8em; color:#666;">Токен станет невидимым для игроков.</p>
            </div>
             <div class="form-group"><label>Поиск (СЛ)</label><input type="number" name="system.attributes.spotDC.value" value="{{attr.spotDC.value}}"/></div>
        </div>
        {{/if}}
    </div>
</form>