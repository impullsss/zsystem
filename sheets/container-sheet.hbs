<form class="{{cssClass}}" autocomplete="off">
    <header class="sheet-header">
        <img class="profile-img-box" src="{{actor.img}}" data-edit="img" title="{{actor.name}}"/>
        <div style="flex:1;">
            <input class="charname-input" name="name" type="text" value="{{actor.name}}" placeholder="Название"/>
        </div>
    </header>

    <nav class="sheet-tabs tabs" data-group="primary">
        <a class="item" data-tab="loot">Содержимое</a>
        {{#if isGM}}<a class="item" data-tab="config" style="color:#d84315;"><i class="fas fa-cogs"></i> GM</a>{{/if}}
    </nav>

    <div class="sheet-body">
        
        <!-- LOOT TAB -->
        <div class="tab loot" data-group="primary" data-tab="loot">
            
            <!-- 1. ЗАМОК -->
            {{#if isLocked}}
                {{#if isGM}}
                    <div style="background:#fff3e0; padding:5px; margin-bottom:10px; border:1px dashed orange;">
                        <i class="fas fa-lock"></i> <b>GM:</b> Игроки видят "Заперто".
                    </div>
                {{else}}
                    <div class="harvest-lock-screen">
                        <i class="fas fa-lock" style="font-size:64px; color:#555; margin-bottom:20px;"></i>
                        <h2>ЗАПЕРТО</h2>
                        <div class="grid grid-3col" style="gap:10px; margin-top:20px;">
                            <button type="button" class="try-key"><i class="fas fa-key"></i> Ключ</button>
                            {{#if attr.canPick.value}}<button type="button" class="try-pick"><i class="fas fa-tools"></i> Взлом</button>{{else}}<button disabled style="opacity:0.5;"><i class="fas fa-ban"></i></button>{{/if}}
                            {{#if attr.canBash.value}}<button type="button" class="try-bash"><i class="fas fa-fist-raised"></i> Выбить</button>{{else}}<button disabled style="opacity:0.5;"><i class="fas fa-ban"></i></button>{{/if}}
                        </div>
                    </div>
                {{/if}}
            {{/if}}

            <!-- 2. СЛОМАНО (НОВОЕ) -->
            {{#if showBrokenScreen}}
                <div class="harvest-lock-screen" style="border-color:#555; background:#cfd8dc;">
                    <i class="fas fa-skull-crossbones" style="font-size:64px; color:#455a64; margin-bottom:20px;"></i>
                    <h2 style="color:#455a64;">ПОВРЕЖДЕНО</h2>
                    <p style="text-align:center;">Механизм уничтожил содержимое.</p>
                </div>
            {{/if}}

            <!-- 3. ЛОВУШКА -->
            {{#if (and (not isLocked) showTrapScreen)}}
                <div class="harvest-lock-screen" style="border-color:#e57373; background:#ffebee;">
                    <i class="fas fa-bomb" style="font-size:64px; color:#c62828; margin-bottom:20px;"></i>
                    <h2 style="color:#c62828;">ЛОВУШКА!</h2>
                    <p style="text-align:center;">Вы замечаете натяжной механизм...</p>
                    <button type="button" class="try-disarm" style="background:#d32f2f; color:white; margin-top:10px; font-size:1.2em;">
                        <i class="fas fa-cut"></i> Обезвредить
                    </button>
                </div>
            {{/if}}

            <!-- 4. ИНВЕНТАРЬ (ГМ ИЛИ УСПЕХ) -->
            {{#if showInventory}}
                
                <!-- GM АЛЕРТЫ -->
                {{#if isGM}}
                    {{#if hasActiveTrap}}
                        <div style="background:#ffebee; border:2px solid red; padding:5px; margin-bottom:10px; display:flex; align-items:center; justify-content:space-between;">
                            <span style="color:red; font-weight:bold;"><i class="fas fa-bomb"></i> ЛОВУШКА АКТИВНА</span>
                            <a class="gm-disarm" style="font-size:0.8em; background:red; color:white; padding:2px 5px; border-radius:3px; cursor:pointer;">GM Disarm</a>
                        </div>
                    {{/if}}
                    {{#if isBroken}}
                        <div style="background:#cfd8dc; border:2px solid #455a64; padding:5px; margin-bottom:10px; display:flex; align-items:center; justify-content:space-between;">
                            <span style="color:#455a64; font-weight:bold;"><i class="fas fa-skull-crossbones"></i> СЛОМАНО (Игроки не видят лут)</span>
                            <a class="gm-fix" style="font-size:0.8em; background:#455a64; color:white; padding:2px 5px; border-radius:3px; cursor:pointer;">Починить</a>
                        </div>
                    {{/if}}
                {{/if}}

                <div class="section-header">
                    <span>Предметы</span>
                    {{#if isGM}}<a class="item-create" title="Добавить предмет"><i class="fas fa-plus-circle"></i></a>{{/if}}
                </div>
                
                <ol class="light-item-list">
                {{#each inventory as |section key|}}
                    {{#each section.items as |item|}}
                        <li class="light-item" data-item-id="{{item.id}}">
                            <img src="{{item.img}}" width="24" height="24"/>
                            <div class="item-name">{{item.name}}</div>
                            <div class="item-qty">x{{item.system.quantity}}</div>
                            {{#if ../../isGM}}
                            <div class="item-controls">
                                <a class="item-edit"><i class="fas fa-edit"></i></a>
                                <a class="item-delete"><i class="fas fa-trash"></i></a>
                            </div>
                            {{/if}}
                        </li>
                    {{/each}}
                {{/each}}
                </ol>
            {{/if}}
        </div>

        <!-- GM CONFIG TAB (Без изменений, но нужно оставить его наличие) -->
        {{#if isGM}}
        <div class="tab config" data-group="primary" data-tab="config">
            <h3>🔒 Замок</h3>
            <div class="form-group"><label>Заперто?</label><input type="checkbox" name="system.attributes.isLocked.value" {{checked attr.isLocked.value}}/></div>
            <div class="form-group"><label>Название Ключа</label><input type="text" name="system.attributes.keyName.value" value="{{attr.keyName.value}}"/></div>
            
            <div class="grid grid-2col">
                <div class="form-group"><label>Можно взломать?</label><input type="checkbox" name="system.attributes.canPick.value" {{checked attr.canPick.value}}/></div>
                <div class="form-group"><label>Сложность (СЛ)</label><input type="number" name="system.attributes.lockDC.value" value="{{attr.lockDC.value}}"/></div>
            </div>
            <div class="grid grid-2col">
                <div class="form-group"><label>Можно выбить?</label><input type="checkbox" name="system.attributes.canBash.value" {{checked attr.canBash.value}}/></div>
                <div class="form-group"><label>СЛ Выбивания</label><input type="number" name="system.attributes.bashDC.value" value="{{attr.bashDC.value}}"/></div>
            </div>
            <div class="form-group"><label>Формула Шума</label><input type="text" name="system.attributes.noiseFormula.value" value="{{attr.noiseFormula.value}}" placeholder="2d6"/></div>

            <hr>
            <h3>💣 Ловушка (Механика)</h3>
            <div class="grid grid-2col">
                <div class="form-group"><label>Установлена?</label><input type="checkbox" name="system.attributes.isTrapped.value" {{checked attr.isTrapped.value}}/></div>
                <div class="form-group"><label>Активна?</label><input type="checkbox" name="system.attributes.trapActive.value" {{checked attr.trapActive.value}}/></div>
            </div>
            <div class="form-group"><label>Формула Урона</label><input type="text" name="system.attributes.trapDmg.value" value="{{attr.trapDmg.value}}" placeholder="3d6"/></div>

            <div class="coverage-table" style="background:rgba(0,0,0,0.05); padding:5px; border:1px solid #999; border-radius:3px; margin-bottom:5px;">
                <label style="font-weight:bold; display:block; margin-bottom:3px;">Зоны поражения (Суммируются!):</label>
                <div class="grid grid-3col" style="font-size:0.9em;">
                    <label><input type="checkbox" name="system.attributes.trapLimbs.head" {{checked attr.trapLimbs.head}}/> Голова</label>
                    <label><input type="checkbox" name="system.attributes.trapLimbs.torso" {{checked attr.trapLimbs.torso}}/> Торс</label>
                    <label><input type="checkbox" name="system.attributes.trapLimbs.lArm" {{checked attr.trapLimbs.lArm}}/> Л.Рука</label>
                    <label><input type="checkbox" name="system.attributes.trapLimbs.rArm" {{checked attr.trapLimbs.rArm}}/> П.Рука</label>
                    <label><input type="checkbox" name="system.attributes.trapLimbs.lLeg" {{checked attr.trapLimbs.lLeg}}/> Л.Нога</label>
                    <label><input type="checkbox" name="system.attributes.trapLimbs.rLeg" {{checked attr.trapLimbs.rLeg}}/> П.Нога</label>
                </div>
            </div>

            <div class="grid grid-2col">
                <div class="form-group"><label>Дист. Триггера (м)</label><input type="number" name="system.attributes.trapTriggerRadius.value" value="{{attr.trapTriggerRadius.value}}" placeholder="1"/></div>
                <div class="form-group"><label>Радиус Взрыва (AOE)</label><input type="number" name="system.attributes.trapDamageRadius.value" value="{{attr.trapDamageRadius.value}}" placeholder="0"/></div>
            </div>

            <div class="grid grid-2col">
                <div class="form-group">
                    <label>Навык для снятия</label>
                    <select name="system.attributes.disarmSkill.value">
                        <option value="mechanical" {{#if (eq attr.disarmSkill.value "mechanical")}}selected{{/if}}>Механика</option>
                        <option value="science" {{#if (eq attr.disarmSkill.value "science")}}selected{{/if}}>Наука</option>
                        <option value="survival" {{#if (eq attr.disarmSkill.value "survival")}}selected{{/if}}>Выживание</option>
                        <option value="lockpick" {{#if (eq attr.disarmSkill.value "lockpick")}}selected{{/if}}>Взлом</option>
                    </select>
                </div>
                <div class="form-group"><label>Сложность (СЛ)</label><input type="number" name="system.attributes.disarmDC.value" value="{{attr.disarmDC.value}}"/></div>
                <div class="form-group"><label>Шум (Обезвреж.)</label><input type="text" name="system.attributes.disarmNoise.value" value="{{attr.disarmNoise.value}}" placeholder="2"/></div>
            </div>

            <hr>
            <h3>👻 Схрон (Скрыто)</h3>
             <div class="form-group"><label>Скрыт на сцене?</label><input type="checkbox" name="system.attributes.isHidden.value" {{checked attr.isHidden.value}}/></div>
             <div class="grid grid-2col">
                 <div class="form-group"><label>Радиус Обнаружения (м)</label><input type="number" name="system.attributes.spotRadius.value" value="{{attr.spotRadius.value}}"/></div>
                 <div class="form-group"><label>Поиск (СЛ)</label><input type="number" name="system.attributes.spotDC.value" value="{{attr.spotDC.value}}"/></div>
             </div>
        </div>
        {{/if}}
    </div>
</form>